// ──────────────────────────────────────────────
// Niri fragment: Look & feel — THEME-OWNED.
// Included by config.kdl. The hexciri theme hook (hexciri-sync.sh) REWRITES
// this file on every theme change, so:
//   • keep it to theme surface only (borders, focus ring, shadow, blur)
//   • don't put spacing/column/strut tweaks here — those live in config.kdl
//   • NOTE the niri include quirk: `layout { border {} }` written here does
//     NOT enable the border by presence alone — each block must say on/off
//     explicitly (that's why they're written out below, never omitted).
// ──────────────────────────────────────────────

// Focus ring, border and shadow are LAYOUT properties — they live inside a
// `layout {}` block that MERGES with the one in config.kdl (gaps, widths).
// NOTE the include quirk: writing `layout { border {} }` in an included file
// does NOT enable the border by presence — each must say `on`/`off` explicitly
// (that's why both are written out below, never omitted).
layout {
    // Focus ring — thin highlight around the active window.
    // off: no ring (it would render as a solid rectangle behind the window and
    // show through semitransparent terminals, hiding blur + covering them).
    // Theme hooks flip this to `on` + a color when a scheme wants it.
    focus-ring {
        off
    }

    // Border — off. niri draws border & focus ring as a solid rectangle behind
    // the window (niri FAQ), so any niri-drawn border covers semitransparent
    // windows and hides the blur behind them. To keep terminals see-through,
    // both border and focus-ring must stay off.
    border {
        off
    }

    // Drop shadow behind windows
    shadow {
        on                                  // Enable shadows (set to "off" to disable)
        softness 30                         // Shadow blur radius
        spread 5                            // How far the shadow extends
        offset x=0 y=5                      // Shadow offset (x=horizontal, y=vertical)
        color "#0007"                       // Shadow color with alpha (0007 = semi-transparent black)
    }
}

// ──────────────────────────────────────────────
// Blur settings
// Controls the blur effect applied by layer rules in window-rules.kdl.
// Higher passes = stronger blur. Higher offset = more spread.
// ──────────────────────────────────────────────

blur {
    passes 3                                // Number of blur passes (more = stronger blur)
    offset 3.0                              // Blur spread distance
    noise 0.02                              // Subtle noise texture
    saturation 1.5                          // Color saturation boost
}

// ──────────────────────────────────────────────
// Animations — the "feel" half of look-and-feel.
// Controls how windows move, open, close, and switch.
// "spring" = physics-based (bouncy feel). "duration-ms" = fixed time.
// damping-ratio: 1.0 = no bounce, <1.0 = bouncy, >1.0 = overdamped
// stiffness: higher = faster/snappier
// ──────────────────────────────────────────────

animations {
    workspace-switch {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001    // Snappy, no bounce
    }

    window-open {
        duration-ms 150
        curve "ease-out-expo"               // Fast start, slows down at end
    }

    window-close {
        duration-ms 150
        curve "ease-out-quad"
    }

    horizontal-view-movement {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001
    }

    window-movement {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001
    }

    window-resize {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001
    }

    overview-open-close {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001
    }

    screenshot-ui-open {
        duration-ms 200
        curve "ease-out-quad"
    }
}

// ──────────────────────────────────────────────
// Hotkey overlay
// The "Important Hotkeys" popup that shows on startup.
// ──────────────────────────────────────────────

hotkey-overlay {
    skip-at-startup                         // Don't show the overlay when Niri starts
    hide-not-bound                          // Hide keybinds that aren't bound to any key
}