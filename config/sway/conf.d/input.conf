# hexciri — input rendered for sway
# source: config/niri/input.kdl (niri fragment; regenerate with: hexciri-config-render input sway)

input type:keyboard {
    xkb_layout us
}
input type:touchpad {
    tap enabled
    natural_scroll enabled
}

