# Extra theme source: Omarchy defaults
# Source of truth for what hexciri seeds on a fresh install. Trim lines to
# drop themes from the picker without removing them from installability.
label = Omarchy
owner = omacom
type = monorepo
branch = quattro
subtree = themes
default = 1