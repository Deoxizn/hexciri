# Extra themes — handpicked omarchy-format themes that aren't shipped.
# One theme per line: <owner>/<name>. "Sync extra themes" clones every line,
# and REMOVES local extras whose line you deleted — the list is the source of
# truth. See config/theme-sources/README.md for how to add a creator.
label = Extra themes
owner = mixed
type = per-theme-repos
default = 0