-- Hexciri live layout (written by hexciri-hyprland-layout, never by hand).
-- Seeded default: dwindle (Hyprland stock default). The cycler rewrites the
-- layout value below on every Mod+L press, so edits here are overwritten.
hl.config({
    general = {
        layout = "dwindle",
    },
})
